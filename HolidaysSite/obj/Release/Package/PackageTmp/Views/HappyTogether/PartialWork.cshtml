@model HolidaysSite.Models.ImageModel.Image
@{
    Layout = null;
}
@foreach (var image in Model.FileServiceDetails.Take(1))
{
    <p class="caption">
        @Html.DisplayFor(modelItem => modelItem.Description) :
    </p>
        <div class="div div2" style="margin-bottom: 30px;">
            @foreach (var item in Model.FileServiceDetails)
            {
                <div class="gallery">
                    <center>
                        <ul class="img-list">
                            <li tabindex="0"><a href="/HappyTogether/GetImage/?imageId=@item.Id"><img class="img_1" src="/HappyTogether/GetImage/?imageId=@item.Id"></a></li>
                        </ul>


                        <div class="lightbox">
                            <span class="cancel"><i class="fa fa-times" aria-hidden="true"></i></span>
                            <div class="overlay"></div>
                            <figure>
                                <span class="prev"><i class="fa fa-angle-left" aria-hidden="true" align="center"></i></span>
                                <span class="next"><i class="fa fa-angle-right" aria-hidden="true"></i></span>
                                <img src="#">
                            </figure>
                        </div><!-- //lightbox -->
                    </center>
                </div>
            }         
        </div>
}

