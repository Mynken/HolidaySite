@model HolidaysSite.Models.ImageModel.Image
@{
    Layout = null;
}

@foreach (var image in Model.FileServiceDetails.Take(1))
{

    <p class="caption">
        <span>
            @Html.DisplayFor(modelItem => modelItem.Description) :
        </span>
    </p>

}

@foreach (var item in Model.FileServiceDetails)
            {

    <li class="item" tabindex="0">
        <a href="/HappyTogether/GetImage/?imageId=@item.Id">
            <!-- <img class="img_1" src="/HappyTogether/GetImage/?imageId=@item.Id">
            <figure class="zoo-item" zoo-scale="2" zoo-image="/HappyTogether/GetImage/?imageId=@item.Id">...</figure>
            -->
            <img class="img_1" src="/HappyTogether/GetImage/?imageId=@item.Id">
            <figure class="zoo-item" zoo-scale="2" zoo-image="/HappyTogether/GetImage/?imageId=@item.Id"></figure>
        </a>
    </li>

}

